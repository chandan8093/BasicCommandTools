# Create a OS in VM 
```
    1. Create a folder and then crate a file name Vagrantfile 
    2. Go to Vagrant websites find boxes then search which os you want to select 
    3. Copy Vagrantfile code and paste in step 1 . 
    4. Go to gitbash and then type
    5. vagrant init . vagrant up , vagrant halt , vagrant ssh
```